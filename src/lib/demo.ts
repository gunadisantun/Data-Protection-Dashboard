export const demoDepartmentName = "Unit ABC";

export function getDemoUserId(sessionId: string) {
  return `demo-user-${sessionId}`;
}

export function getDemoDepartmentId(sessionId: string) {
  return `demo-dept-${sessionId}-unit-abc`;
}
